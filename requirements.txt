# Core pipeline (jrl_impute.py) and tests
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
scikit-image>=0.21
matplotlib>=3.7
pytest>=7

# Legacy scripts only (not needed for jrl_impute.py):
#   nn_model.py, simulate_demo_variable_psf.py -> keras 2 / tensorflow (keras.wrappers.scikit_learn)
#   simulate_demo*.py                          -> pandas
#   airy_get_psf.py                            -> pims, local bead data
